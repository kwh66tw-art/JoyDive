# JD2-Logbook v1.2 Backlog（待辦追蹤）

> 建立日期：2026-07-20。v1.1 backlog（`V1_1_BACKLOG.md`）已完工，本檔案是下一輪工作的
> 追蹤清單。原 15 項多數已完成；2026-07-25～26 語系全審核（#6/#12/#13/#15～#17/#22/#23）
> 收尾期間，逐項核對第三方稽核報告與真機回報又額外發現並修復多個真實 bug（Trimix
> 存檔資料損毀 #18、單位換算缺口 #18/#20/#23、`ImportWizardView` 語系完全失效 #21 等）——
> 目前僅剩：#6 語系全審核最後一輪 rev05（見 #16）、少數需要母語審核才能定案的翻譯用詞
> （希臘文/克羅埃西亞文 No Deco，見 #23）、以及 #19/#21 稽核報告中標記為「需使用者決定」
> 的架構/合規類項目（ATS 設定、GDPR 同意機制、SwiftData migration plan 等）。
> **單一權威來源**：本輪工作以此檔為準，`Docs/KNOWN_ISSUES.md` 只放精簡清單＋連結於此。
> **最後更新請見 `git log -- V1_2_BACKLOG.md`**（不再手動維護日期戳，理由見下方「使用方式」）。

---

## 待辦清單

| # | 項目 | 狀態 | 細節 |
|---|------|------|------|
| 1 | iOS 版送審被拒絕，擬訂下次送審對策 | ✅ 已修正並送出審核（2026-07-28，iOS + macOS 皆 Waiting for Review） | 收到完整拒絕內容：2.3.6（Advertising 未勾選，Age Rating 需改 Yes）+ 5.1.2(i)（App Privacy 標示 Coarse Location/Other Usage Data/Device ID 用於追蹤，但程式碼查核確認**未使用 ATT、未用 IDFA、AdMob 為預設非個人化廣告、CoreLocation 僅本機一次性定位**，判定為 ASC 隱私標籤誤填，非真的有追蹤行為）。已修復 `logbook/privacy.md` 三語（EN/繁中/日文）內文，移除與實際行為不符的敘述。**2026-07-25 記錄「已在 ASC 後台完成」，但 2026-07-28 直接在 ASC 逐格核對發現記錄與實際不符**：Coarse Location/Device ID/Usage Data 三格的「Purpose」問卷仍勾著「Third-Party Advertising」（Apple 定義下此勾選即等同宣告追蹤，是與「used to track」不同的獨立問卷欄位，07-25 的操作應是漏改了這一格），5.1.2(i) 實際上直到 07-28 才真正修正完成。07-28 重新逐格取消「Third-Party Advertising」、改勾「App Functionality」，Linked-to-identity／Tracking 兩問皆確認 No，Publish 後重新整理頁面截圖驗證。Age Rating Advertising=Yes、Category=Health & Fitness 二次核對確認無誤。**新增**：Declare Regulated Medical Device 聲明回答 No（App 不構成醫療器材）。**送審動作**：Build 3 (v1.2) 上傳並綁定、Export Compliance 回答完成、三語 App Store 文案（Description/Keywords/Promotional Text/What's New）套用至 iOS + macOS 兩平台、Review Notes 註明修正內容，iOS + macOS 皆已 Add for Review / Resubmit，狀態為 Waiting for Review。**Lessons learned 詳見** `docs/reports/R-2026-07-28-iOS送審駁回二次核查與修正.md`。 |
| 1b | 「開啟 Game Center」誤設定（iOS + macOS 皆受影響） | ✅ 確認修復（iOS + macOS 皆已驗證，ASC Category 欄位也已同步改） | 查證：程式碼內完全沒有 GameKit/GameCenter API 或 entitlement，根因是共用的單一 `Info.plist`（iOS/macOS 雙平台共用）內 `LSApplicationCategoryType = public.app-category.sports-games`——這是 Apple 官方「Games → Sports」子分類，會讓系統把本 App 當成遊戲，自動觸發 iOS 18+/macOS 的 Game Mode／Game Center 相關行為（使用者截圖證實 iOS 端也出現「遊戲模式：已開啟」橫幅，非原先以為的 macOS-only）。已改為 `public.app-category.healthcare-fitness`（無遊戲關聯的正確分類），iOS + macOS build 皆通過。**2026-07-25 macOS 端追查發現比預期複雜**：使用者重新 build 裝上真機後，Xcode 偵錯階段跟 `/Applications` 正式安裝的版本都確認 `Info.plist` 已是 `healthcare-fitness`，但選單列的火箭圖示（點擊以前會顯示 Game Center 內容，現在點擊已無內容）仍然存在；**關鍵判斷依據**：使用者接著測試「完全沒動過、直接從 App Store 下載的既有 v1.0 正式版」，同樣出現火箭圖示且點擊無內容，證實這個現象**跟這次改的本機程式碼無關**（該版本從未套用過這次的 Info.plist 修復），圖示會隨 `Cmd+Q` 結束程式一起消失，證實是這個 App 觸發的系統 UI、非無關的第三方選單列工具。**研判**：macOS 對「已發佈 App Store 版本」是否為遊戲的判斷，很可能讀的是 **App Store Connect 伺服器端登記的 Category**，不是單純看本機 `Info.plist`——這正是本項原本就記錄的「待使用者於下次送審時同步處理 ASC 後台 Category 欄位」，現在有了具體的行為證據佐證這一步是必要的、不只是為了資料一致性。**2026-07-25 最終釐清並確認修復**：先前判斷「需要送審核准才能解決」是想太多——iOS 端當初改完 `Info.plist` 重新 build 裝上真機就直接解決，macOS 應該是同一套機制（讀 App 自己內嵌的 `Info.plist`，非讀 ASC 伺服器端資料）。真正關鍵：使用者指出火箭圖示「只有透過 Xcode `cmd+R` 執行才出現」，改成**直接在 Finder 雙擊 `/Applications/JoyDive².app` 開啟**（不透過 Xcode），火箭圖示確認消失——證實是 Xcode 偵錯階段本身的產物（開發簽署/`get-task-allow` 之類），跟 Game Mode／Category 完全無關，不會出現在使用者從 App Store 安裝的正式版上。**iOS + macOS 皆已確認修復生效，不需要等這次送審核准**；ASC 後台 Category 欄位已同步改成 Health & Fitness（存粹是為了資料一致性，非解決 Game Mode 的必要條件） |
| 1c | 匯入「File X of N」進度與「N 個 dives imported」數字不一致 | ✅ 完成（補充說明文字，非改動計數邏輯） | 使用者回報樣本檔匯入顯示 22 個檔案，匯入完後卻顯示 24 dives。核對 `ImportWizardView.swift` 的 `Text("File \(current) of \(total)")`，`total` 是**檔案數**；`ImportCoordinator.swift` 的 `successCount += result.dives.count` 則是**潛水筆數**——部分格式（如 Suunto DM5/SML XML、Diving Log 6.0 `.db`）單一檔案可能是整本「潛水日誌匯出」，內含多筆潛水，屬預期行為，非 bug。**採納使用者建議**，但保留原有已在 18 語言完整翻譯的「File X of N」字串不動（避免捨棄既有翻譯），改為在其下方新增一行說明文字「Some files may contain multiple dives.」（新 key，暫僅英文，其餘語言待 #6 語系全審核補齊），提前告知使用者數字可能不對等，避免誤以為 bug。iOS + macOS build 通過 |
| 1d | Trimix 潛水無法互動顯示組織艙殘氮（tissue saturation） | ✅ 完成（狀態列恢復互動，只隱藏真的沒有的資料；根本限制仍屬 DiveKit 家族層範圍） | 使用者截圖的 trimix 潛水（Lake Coleridge）點擊/拖曳剖面圖後看不到任何狀態列。核對 `DiveReplayEngine.swift:65-70`：trimix 潛水的 `ReplayPoint` 其實**depth/time/waterTemp 都是有算的**（單純深度/時間/溫度重放，不需要減壓生理），只有 `ceilingDepth`/`ndlSeconds`（恆為 0）跟 `tissuePressures`（空陣列）因為 DiveKit 的 Buhlmann 引擎沒有氦氣隔室模型（`ndlSeconds(at:gasMix:)` 對 trimix 會 `assertionFailure`）才拿不到。**原本的實作誤把整列都隱藏了**（`if replay.decoDataUnavailable { 免責聲明 } else { calloutRow + tissue bars }`，等於連 Time/Depth/Temp 都被免責聲明取代），**依使用者指示修正**：`calloutRow` 固定 5 欄排版（Time/Depth/Temp/Ceiling/No Deco），不因資料缺漏而增減欄位數（維持畫面一致性）——trimix 時 Ceiling/No Deco 欄位改顯示「—」佔位，且不觸發黃色/紅色警示膠囊（避免恆為 0 的假資料被誤判成「快超過免減壓時間」）；Temp 欄同樣固定顯示，樣本沒有溫度時也用「—」佔位，不再整欄消失；組織艙長條圖（`TissueBarsView`）維持用帶 `info.circle` 圖示的說明文字取代，明確告知這是已知限制、非程式錯誤。**根本限制（氦氣組織負荷計算）依家族鐵律第 8 條（回報鏈）**：這是共用 DiveKit 演算法能力缺口，影響 3 個 App，不得在 Logbook 單方面繞道實作，需回報總指揮評估是否列入 DiveKit v2.0 roadmap。iOS + macOS build 通過 |
| 1e | 狀態資訊列文字大小忽大忽小，同裝置同設定下也會不一致 | ✅ 完成 | 使用者回報兩張截圖（trimix 潛水 vs 一般潛水）的 Time/Depth/Temp/Ceiling/No Deco 文字大小明顯不同，確認並非系統文字大小設定差異（同裝置同設定仍不一致）。核對 `calloutCell`，找到兩個各自獨立、會疊加放大問題的成因：①`Text(verbatim: value).minimumScaleFactor(0.7)`——5 欄各自依「自己的字串是否超出所分到的等寬欄位」獨立決定縮放比例，不同潛水的數值字串長度不同就可能讓同一列裡各欄縮放比例不一致；②外層 `VStack` 掛的 `.animation(.easeInOut(duration: 0.15), value: selectedIndex)` 對整個狀態列（含插入時的過渡）生效，若畫面剛好在動畫還沒跑完時被截圖，文字會停在過渡中間的縮放狀態，跟動畫結束後的最終樣式不一致。修法：①移除 `minimumScaleFactor`，改用固定不縮放的 `.footnote`（在最窄的支援機型 iPhone SE、5 欄等寬版面下仍放得下目前最長的數值字串，不需要靠縮放救援）；②`calloutRow(point)` 加 `.transaction { $0.animation = nil }`，讓狀態列一律立即以最終樣式出現，不會有動畫中間態被截到的機會。iOS + macOS build 通過；**受限本機 Xcode 環境問題（Simulator MCP 工具回報 `xcode-select` 未指向 Xcode.app，需要使用者手動跑 `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer` 才能用 attach/tap 做即時視覺驗證），這次修復是基於程式碼靜態分析、未能截圖比對修復前後畫面，待使用者執行該指令或自行在裝置上覆核** |
| 2 | App 內 icon 全盤 review | ⏸️ 延後至下一版 | 使用者指示：這版要趕 3 天內上架，icon review 非上架阻塞項目，留到下一版再處理 |
| 3 | 修正潛水剖面圖互動功能問題 | 🔄 部分完成（時間軸偏移已修復；警示標記/狀態列第二列已建置但先隱藏） | ①**時間軸偏移**——根因是 `chartOverlay` 直接拿 `proxy.position(forX:)`/`proxy.value(atX:)` 當 GeometryReader 座標用，沒扣掉 Swift Charts 左側 Y 軸刻度標籤寬度；改用 `geo[proxy.plotAreaFrame]` 校正 origin。已修復，維持生效。JD2-Ultra companion 同款 bug，已記錄 `SYNC_TO_JD2-ULTRA.md` #6。②**狀態資訊列第二列**（警示事件卡片）與③**曲線警示標示**（紅點=上升過速／橘點=強制安全停留）——`DiveReplayEngine.replay()` 的上升速度追蹤邏輯（比照統一 DiveKit `AlgorithmConstants.maxAscentRateWarn` 等門檻，非另外發明數值）與對應 UI 皆已完整實作並通過 build，但**使用者決定「先 mark 掉、UI 隱藏這 2 個功能，之後改版再決定最後怎麼呈現」**。已在 `DiveAnalysisView.swift` 加入 `private let showWarningEvents = false` feature flag（含說明註解），同時 gate 掉 `selectedWarnings` 計算與曲線 `ForEach` 標記渲染；程式碼與偵測邏輯保留，未刪除，日後只需把常數改回 `true` 即可重新開放。⚠️ 已知遺留 bug（尚未修）：`warningTitle`/`warningDetail` 用 `String(localized:)` 而非 `languageManager.localized(_:)`，不吃 App 內語言切換器，功能重新開放前需一併修正 |
| 4 | 公制／英制單位設定 | ✅ 完成 | `JD2Core/Models/UnitSystem.swift`（metric/imperial 列舉 + 雙向深度/溫度轉換函式：顯示層 `convertDepth`/`convertTemperature`/`formatDepth`/`formatTemperature`，輸入層反向 `metersValue(fromDisplay:)`/`celsiusValue(fromDisplay:)`）+ Settings 頁單位切換（符號呈現：`m / °C` vs `ft / °F`），`@AppStorage` 持久化，儲存值永遠是公制。已全面套用至：`DiveRowView`、`DiveLogDetailView`（含 `DiveStatCell` 兩處）、`DiveAnalysisView`（`calloutRow` 深度/溫度/天花板）、`DiveSiteAnnotation`/`DiveMapRepresentable`（非 SwiftUI View context，改直讀 `UserDefaults`）、`DiveSiteSheetView`（含 `keyStatsRow` 兩處 `DiveStatCell`）、`DiveLogEditSheet`（新增 4 個雙向 `Binding<Double>`/`Binding<Double?>` computed property：`maxDepthDisplay`/`waterTemperatureDisplay`/`airTemperatureDisplay`/`visibilityDisplay`，輸入欄位單位符號與 accessibility label 一併改為動態）。`Extensions.swift` 內的 `depthFormatted`/`DepthLimitCheck.displayDepth` 經查證為零呼叫端死碼，未在此次範圍內處理。新增 4 個 `%@` 格式的 accessibility label 翻譯 key（僅 en，其餘語言待 #6 語系全審核時補齊）。iOS + macOS build 皆通過 |
| 5 | 匯入情境測試 | ✅ 完成，5 情境全數實機跑過並確認 PASS | 使用者指示在 `_JD2-family/00_Import_test_scenes/` 建 5 個子資料夾放測試檔。已利用既有 `00_Import_samples/`（已驗證的 14 格式匯入樣本庫）與 `dive-log-samples/`（家族原始樣本庫）真實/已註明來源的檔案組出 5 種情境，並寫入 `00_Import_test_scenes/README.md` 說明各情境測試方式與預期結果：**ITS_01** 黃金路徑（5 種格式各 1 筆潛水，5 檔→5 筆）；**ITS_02** 單檔多筆潛水（3 個 Subsurface XML/`.ssrf`，3 檔→50 筆，直接復現先前「22 檔→24 筆」情境，數字差距拉大更好驗證，順便確認新加的「Some files may contain multiple dives.」提示文字有顯示）；**ITS_03** 批次匯入 8 種格式混合（Suunto SML/SDE、Divesoft DLF、DAN DL7、Garmin FIT、Shearwater、DivingLog SQLite、Subsurface CSV，測進度條與格式自動偵測）；**ITS_04** 重複匯入／錯誤處理（同內容改檔名測去重、截斷 XML／亂碼內容／空檔案三種壞檔測錯誤處理不中斷整批）；**ITS_05** 特殊情況（真實 trimix 雙潛水 UDDF 樣本測 v1.2 剛修好的 trimix 剖面圖顯示、ZIP 包裝 UDDF 測 `MinimalZipReader`、中文檔名、Mares Dive Organizer 測未支援格式友善拒收、不相干副檔名）。後續使用者要求檢視 `dive-log-samples/`／`divelog-mock-dataset/` 還有無可用素材，找到 5 項有價值但先前未涵蓋的，追加進 ITS_04／ITS_05（不新增資料夾，維持 5 情境架構）：Divesoft DLF **v2 header**（`DivesoftDLFParser` 程式碼明確拒收的變體，先前只有單元測試測過，這次補 E2E UI 驗證）→ITS_04；`GarminConnectJSONParser`**從未被任何資料（含 mock）驗證過**的首次測試資料→ITS_05；Deepblu（格式已於 2026-07-19 正式移除支援，測乾淨拒收無殘留邏輯）→ITS_05；Seac 品牌（另一個無解析器支援的真實樣本，補充不支援格式多樣性）→ITS_05；ATMOS Mission3 跨品牌 UDDF＋FIT（探索性測試，先前曾發生過 ATMOS 的 FIT 被誤當 Garmin FIT 收錄的教訓，這次直接驗證 `GarminDescentParser` 對非 Garmin 品牌 FIT 檔的實際行為）→ITS_05。`00_Import_test_scenes/README.md` 已同步更新各項測試方式與預期結果。使用者實測回報 3 項結果，逐一查證：**① ITS_03「10 筆潛水不是 8 筆」**——非 bug，`dive_2026-06-01.zxu`（DAN DL7）樣本內含 3 組 ZDH/ZDT（`DANDL7Parser.swift` 程式碼明確支援單檔多潛水），8 檔案−1+3=10 筆為正確結果，先前 README 的「8→8」預期算錯，已更正。**② ITS_05「zip 檔匯入失敗」**——非 bug，是測試檔案命名錯誤：`UDDFParser.canHandle()` 只認副檔名字面是不是 `.uddf`，不會對 `.zip` 副檔名做內容嗅探，ZIP 解壓邏輯本身沒問題但要求維持 `.uddf` 副檔名（同 Suunto SDE 格式的慣例）；已把測試檔案從 `.zip` 改名回 `.uddf`。**③ ITS_04 語系問題**——**真的是 bug，兩個獨立成因**：(a) App 層 `Localizable.xcstrings` 的 `"Failed Files"` key **完全沒有任何語言的翻譯值**（連英文都沒有，`locales: NONE`），導致不論 App 設什麼語言都直接顯示英文 key 本身，已補上 en／zh-Hant（其餘語言待 #6 語系全審核補齊）；(b) **更嚴重**：截圖裡「解析失敗: ...」「不支援的格式: ...」這些錯誤訊息本身，不論 App 語言為何都固定顯示繁體中文——查證後這些字串是**寫死在家族共用 `DiveImportKit`**（14 個檔案都有這個問題，非單一遺漏），完全沒有走任何翻譯機制。**依家族鐵律第 8 條，這是跨 3 個 App 的 Kit 層問題，已停下回報，不在 Logbook 單方面繞道處理**：已寫成正式報告 `_JD2-family/reports/R-2026-07-25-DiveImportKit錯誤訊息未本地化.md`（含完整 34 處程式碼位置列表、根因分析、3 種修法方向建議），並登錄進 `_JD2-family/F-00-文件登錄表.md`，供接手處理 DiveImportKit 的 session／agent 直接使用（單一戰場規則，同時間限一個 session 動 Kit）。`00_Import_test_scenes/README.md` 已同步更正 ITS_03／ITS_05 的預期結果說明。iOS + macOS build（Logbook 層修復部分）皆通過 |
| 6 | 語系全審核 | 🔄 進行中（第一輪 260 key 已審完套用，第二輪 13 個新增 key 已交付待審） | 原訂排在最後才做，但 coding 項目（#1/#1b/#3/#5 等）已全部完成，實際已提前開始：見 #12（CSV 匯出）／#13（第一輪審閱結果套用）／#15（第二輪僅新增 key 的追加審閱）。ASC 後台動作（#1 Age Rating/Privacy/Category）已提前完成，非等語系審完才做——當初「排在最後」的排序被使用者自己的實際節奏取代，此列僅追蹤語系審核本身還沒收尾 |
| 7 | Import tab 部分文字未跟隨 App 內語言切換 | ✅ 完成 | 使用者發現日文語系下 Import tab 仍顯示英文（「Select Files or Folder」「Supported Formats」「UNIVERSAL」等）。根因與 v1.1 #8 相同類型：`ImportWizardView.swift` 用 `String(localized:)`（純 Foundation API，只認系統語言）而非 `languageManager.localized(_:)`（吃 App 內語言切換器），只是 v1.1 #8 修復當時這批 Import 格式清單程式碼還沒寫。全面替換為 `languageManager.localized(_:)`，新增缺漏的 "Import Completed with Issues" 翻譯 key（18 語言） |
| 8 | macOS 地圖沒有 recenter 按鈕 | ⚪ 非問題（使用者確認） | 查證：`recenterButton` 程式碼本身沒有 iOS-only 限制，只受 Settings 頁「Enable GPS Location」開關（`gpsLocationEnabled`）控制，該開關未開時按鈕本來就不存在——macOS/iOS 是各自獨立的 App 安裝，UserDefaults 不共用。使用者確認是 macOS 版沒開定位開關，非程式 bug，跳過 |
| 9 | 真機驗證：AdMob 廣告 / IAP 購買流程 / Restore Purchase | ✅ 完成，全數 PASS | **IAP 購買流程**：使用者截圖確認 Sandbox 購買彈窗正確顯示「Sandbox」環境標籤＋「僅供測試使用，確認此購買交易不會導致扣款」，帳號 kwh2006@gmail.com，✅ **PASS**。**Restore Purchase**（正向：已購買帳號重灌後還原）：✅ **PASS**。**AdMod 視覺驗證**：`AdBannerView.swift` 的 4 個 Ad Unit ID 皆為正式 ID，程式碼原本沒有註冊 `testDeviceIdentifiers`，真機會直接請求正式廣告，有誤觸真實廣告造成 Google 判定無效流量的風險。使用者提供 Xcode Console 印出的裝置 ID（`a10efcaff62afcacbd5dc5c884fb7f6d`），已在 `JD2_LogbookApp.swift` 的 `init()` 加入 `#if DEBUG` 包住的 `MobileAds.shared.requestConfiguration.testDeviceIdentifiers = [...]`，讓真機在 Debug build 下也拿到官方安全測試廣告，可放心逐頁（Logbook/Import/Settings）檢查版位是否正常顯示、不遮擋內容；Release/Archive 送審 build 不受影響，一律走正式廣告請求。**第一輪失敗**：使用者裝上第一組 ID 後 Settings 頁仍顯示真實廣告（非 Test Ad），核對後發現 Xcode Console 重新印出的裝置 ID（`eb621500356405187fcd4d978b8c6131`）跟第一次拿到的（`a10efcaff62afcacbd5dc5c884fb7f6d`）不一樣——判斷第一組 ID 來源可能是模擬器 session 或不同裝置，對這台真機無效。已把兩組 ID 都加進陣列（`testDeviceIdentifiers = [...]`），iOS + macOS build 皆通過。重新 build 裝上真機後 Settings 頁已確認出現「Test mode」，✅ **AdMob 測試裝置註冊 PASS**（Logbook/Import/地圖空狀態版位使用者尚未逐一回報，視為同一支 App 內共用同一組 request configuration，risk 低）。**附帶發現並修復**：使用者回報 Settings 頁「Developer Tools」的「Simulate Premium (No Ads)」開關每次啟動都自動變回 ON，干擾廣告解鎖測試。查證 `SettingsView.swift` 的 Toggle 綁定 `get: { purchaseManager.isPremium }`（真實 StoreKit 授權狀態）／`set: { purchaseManager.setDebugPremium($0) }`，而 `PurchaseManager.refreshPurchaseStatus()` 每次啟動都會用 `Transaction.currentEntitlements` 重新核對真實購買紀錄——使用者測試 IAP 時完成的真實 Sandbox 購買，讓這個帳號從此永遠有效授權，`DEBUG_isPremiumOverride` 只能強制設為 true、無法強制蓋掉真實授權設為 false，導致開關永遠被真實購買狀態覆蓋回 ON，並非隨機故障。**依使用者指示直接移除**：`SettingsView.swift` 拿掉整個 Toggle；`PurchaseManager.swift` 一併移除已無呼叫端的 `setDebugPremium()`／`DEBUG_isPremiumOverride` 相關程式碼（避免留下死碼）。iOS + macOS build 皆通過。**Restore Purchase 反向情境**：使用者在 ASC 對 `kwh2006@gmail.com` 執行 Clear Purchase History 後重灌 App，仍顯示「進階版已解鎖」，詢問是否程式碼沒設好。查證後判斷**非程式碼問題**，是 StoreKit 2 Sandbox 已知的本地快取行為：`Transaction.currentEntitlements`（`PurchaseManager.refreshPurchaseStatus()` 用來判斷購買狀態）讀的是**裝置端** StoreKit 交易快取，此快取跟著「目前登入的 Sandbox 帳號」走、不存在 App 自己的容器內，單純刪除重裝 App 不會清掉；ASC 的 Clear Purchase History 只清**伺服器端**紀錄，裝置本地快取要重新跟伺服器對一次（即 `AppStore.sync()`，也就是 Restore Purchase 按鈕本身做的事）才會更新。已回覆使用者建議流程：① 確認裝置上真的完全刪除 App（非 Xcode Run 覆蓋安裝）；② 裝置上把 Sandbox 帳號登出再登入一次；③ 重裝 App；④ 冷啟動可能仍顯示已解鎖屬預期（尚未強制同步）；⑤ 手動點 Restore Purchase（觸發真正的 `AppStore.sync()`）才是正確的驗證時機點，若這步之後仍顯示已解鎖才需要回頭查程式碼。**✅ 使用者已依此流程重測，PASS**：Clear Purchase History 後用 Sandbox 測試帳號點 Restore Purchase，確定無法還原（正確顯示沒有可還原的購買）；重新購買一次，確定又能正常移除廣告。**IAP／Restore 全部情境（正向購買、正向 Restore、反向 Restore、重複購買後再 Restore）至此全數 PASS**。**AdMob 測試裝置 ID 每次都不同**：使用者發現重測 Restore Purchase 時（需要整個刪除重裝 App）Google Console 印出的裝置 ID 每次都不一樣，追加不完。根因：Google 的裝置測試 ID 雜湊值跟著 `identifierForVendor`（IDFV）算，而 IDFV 有「該開發商所有 App 都從裝置移除後，下次重裝會重新產生」的特性，使用者這幾輪測試剛好一直在整個刪除重裝 App，導致 ID 一直變、用 `testDeviceIdentifiers` 註冊特定裝置 ID 的做法從根本上就跟這個測試情境衝突。**改用更根本的解法**：`AdBannerView.swift` 的 `AdUnitID` 改成 `#if DEBUG` 時直接用 Google 官方測試 Ad Unit ID（`ca-app-pub-3940256099942544/2934735716`，四個版位共用，Google 允許重複使用，不綁裝置、永久有效、不需要任何註冊步驟），Release/Archive 送審 build 維持原本 4 個正式 Ad Unit ID 不受影響；`JD2_LogbookApp.swift` 移除已無意義的 `testDeviceIdentifiers` 註冊程式碼。iOS + macOS build 皆通過。**✅ 使用者已重新 build 並逐一確認 Logbook/Import/Settings/地圖空狀態四個版位廣告皆正常顯示（「廣告版位都看過，沒問題」）**，AdMob/IAP/Restore Purchase 三大項至此全數驗證完畢 |

---

| 10 | 本機開發環境清理（macOS Launchpad 殭屍 App 記錄／舊 DerivedData） | 🔄 部分完成，2 個 `.xcarchive` 待送審過關再清 | 使用者發現 Launchpad 搜尋 `joy`／`JD` 跳出一堆重複、圖示打叉的 App（`JoyDive².app`／`JD2Ultra.app`／`JD2Immersion.app` 等）。查證後是本機 `LaunchServices` 資料庫累積的殭屍註冊（指向已刪除的模擬器/XCTestDevice 暫存路徑、舊 DerivedData），以及真的存在但佔空間的舊建置產物。**已清除**：29 筆殭屍 LaunchServices 註冊（`lsregister -u`，純快取清理零風險）；`/Users/kevin/Library/Developer/Xcode/DerivedData/JD2-Logbook-apdlqmncpwjhkqgndqrwcujcdgjp`（558M，舊 Logbook DerivedData）；`/Users/kevin/Documents/iosApp/JoyDive/`（193M，改名前的舊專案，使用者確認可全清）；`JD2Immersion-bxaqokdhqeirshgzkjiwfoxphprq`／`JD2Ultra-brwgjfsvawrnregrasbfeypbgcez`／`JD2Ultra-gfsenpypvxiccwctcnvnzilpgjds` 三個 JD2-ultra／JD2-immersion 的 DerivedData（共約 2GB，使用者確認可清）。**⚠️ 使用者明確指示保留、待這輪送審過關後才清**：`/Users/kevin/Library/Developer/Xcode/Archives/2026-06-17/` 底下 2 個 `.xcarchive`（`JD2-Logbook 2026-6-17, 09.27` 與 `10.36`），內含 v1.0 時期 build 的 dSYM，保留供日後 App Store 使用者 crash 回溯用；**送審核可後記得回來清掉**。另外，`/Applications/JoyDive².app` 這份舊安裝（7 月 3 日、內含未修復的 `sports-games` 分類）已重新 build 並由使用者用 `sudo` 覆蓋更新 |

| 11 | Export/Import Backup 功能尚未測試，先隱藏 | ✅ 完成 | 使用者發現這個 v1.1 #14 做的功能這輪還沒實際測過（無論是單元測試以外的真機 round-trip），決定這版先隱藏、下一版驗證過再開放。`SettingsView.swift` 加入 `private let showBackupSection = false` feature flag（同 v1.2 #3 的 `showWarningEvents` 手法），把整個 Backup Section（Export/Import 兩顆按鈕）包進 `if showBackupSection { ... }`；程式碼與底層邏輯（`exportBackup()`/`importBackup(from:)`/`BackupJSONDocument`）完全保留未刪除，日後只需把常數改回 `true` 即可重新開放，不需要改動其他任何地方。iOS + macOS build 皆通過 |
| 12 | 語系全審核（#6）— CSV 素材已備妥 | 🔄 進行中，CSV 已產出待審 | 從 `Localizable.xcstrings`（260 個 key × 18 語言）匯出成 CSV，額外加一欄「使用情境」：優先沿用 xcstrings 既有的 `comment`，沒有的話用程式碼 grep 找出該 key 出現在哪些 Swift 檔案自動補上定位線索（例如「None」同時出現在 `DiveLogDetailView.swift`/`DiveLogEditSheet.swift`，可能對應天氣/氣體/裝備等不同下拉選單，提醒審核時留意每個出現位置語境是否一致），完全查無引用的標警示（可能是動態組字串）。已交付使用者審閱。**⚠️ 待使用者逐語言審完回報，再回頭修正翻譯品質有問題的 key** |

| 13 | 語系全審核 CSV 審閱結果已套用 | ✅ 完成（含一項架構建議記錄待後續） | 使用者提供翻譯團隊審閱過的 `JD2-Logbook_i18n_Completed_0725_rev02.csv`，已寫回 `Localizable.xcstrings`（24 個 key、209 個儲存格更新，含補齊先前 v1.2 這輪新增但僅英文的 stopgap key 之其餘語言翻譯）。編碼為 `utf-8-sig`，泰文/越南文變音符號抽查完整無亂碼。iOS + macOS build 皆通過。**翻譯團隊另附架構建議**：`%lld dive%@ imported`／`%lld skipped (duplicates)` 兩個動態數量字串，目前是 Swift 端算好 `""`/`"s"` 後綴塞進 `%2$@` 的手動 hack，建議改用 `.xcstrings` 原生「Vary by Plural」（`%#@variable@` 語意化格式＋各語言 Zero/One/Two/Few/Many/Other CLDR 規則）——**這項建議判斷正確但本次不動手**：這類多語言 plural 規則正常要靠 Xcode String Catalog 編輯器 UI 逐語言生成/驗證骨架（例如 `hr` 克羅埃西亞語有 one/few/many/other 四類，跟英文的二分完全不同），沒有 Xcode 介面驗證、純手刻 JSON 對 18 種語言逐一填對規則風險太高，可能填錯導致退回英文或編譯失敗。**留給有 Xcode 在手邊的人透過編輯器 UI 執行**，不建議之後也用純文字/腳本方式硬改；已寫成家族報告 `_JD2-family/reports/R-2026-07-25-多語系Vary_by_Plural規則需求.md`（含是否值得抽共用層的三個選項供總指揮評估），登錄進 `F-00-文件登錄表.md` |
| 14 | 移除 `project.pbxproj` 的 `ar`（阿拉伯語）殭屍註冊 | ✅ 完成 | 使用者要求移除，查證 `Localizable.xcstrings` 裡完全沒有任何 `"ar"` 翻譯內容、也沒有 `ar.lproj` 資料夾——`ar` 只出現在 `project.pbxproj` 的 `knownRegions` 建置設定裡（疑似 Xcode 專案範本殘留，非刻意加入的語言支援），移除該行。iOS + macOS build 皆通過 |
| 15 | 語系 CSV 追加審閱：僅輸出 v1.2 這輪新增的 key | ✅ 完成 | 使用者要求只給「上次定版送審後新增」的 key，避免重新審核已經審過的 260 個。用 `git show 65c3be1:.../Localizable.xcstrings`（v1.2 這輪開工前最後一次 commit，即上次送審後的基準狀態）跟目前版本做 key 集合差異比對，精確抓出 13 個新增 key（皆為這輪新增功能的翻譯字串：單位系統 accessibility label、Import 多潛水提示、trimix 說明文字、警示標記文案等），非用猜測/日期範圍框列。已交付 `JD2-Logbook_v1.2新增key_0725.csv` |
| 16 | 語系全審核：rev05 套用 + 4 家 AI 交叉審核 + 資料損毀修復 | ✅ 完成（3 項使用者裁定已套用） | 套用 `..._260筆_0725_v2-rev05.csv`（115 key／432 儲存格）。用 4 份 AI 審核建議（`語系修正建議彙整.xlsx`，ms/ppl/op46/gem31p 四個分頁，591 筆建議，432 筆已被 rev05 涵蓋）交叉驗證，**發現並修復 rev05 本身帶進來的資料損毀**：3 處英文（源語言）格式參數字串損毀（如 `%@, %@` 的 en 值變成裸露的 `%2$`，會在畫面顯示亂碼或漏參數）、3 句德文/越南文長句被從中間截斷（只剩後半句）、1 格審核者的編輯備註（`it 維持 kg`）被誤存成正式翻譯值——皆已修復並用自動化腳本掃過全部 260 個 key 驗證格式參數與截斷情況，確認乾淨。另外套用 4 項可獨立驗證成立的建議（zh-Hans 用「设备」非「装置」、「条」非「笔」；hr「uron」與全 App 既有用法一致）。**5 項待使用者裁定，已於 2026-07-26 由使用者裁定 3 項＋提供 `Translation_Review_Report.md`（見下方詳列），全數套用完畢，iOS + macOS build 皆通過** |

### 翻譯疑慮裁定紀錄（V1_2_BACKLOG #16，2026-07-26 完成套用）

1. **"Min NDL"（zh-Hant/zh-Hans）**：使用者裁定 →「最短 NDL」（兩個中文變體皆同一詞，因為是「最短」+ 英文縮寫的混合寫法，簡繁無差異）。原本 3 個 AI 審核者建議不同（ms「最低免停留極限」/op46「最小免減壓極限」/ppl「最低 NDL」），皆未採用。
2. **Trimix 說明文字精簡（zh-Hant/zh-Hans）**：使用者裁定 → 移除冗餘的「混合氣體/混合气体」字樣，改為「Trimix 潛水暫不支援組織氮負荷計算 — 此為已知限制，非錯誤。」（zh-Hant）／「Trimix 潜水暂不支持组织氮负荷计算 — 这是已知限制，不是错误。」（zh-Hans），沿用半形" — "破折號（非 ppl 原建議的全形「——」）。**已核對其餘亞洲語系（ja/ko/th/vi）同一句翻譯**：這幾個語言原本就直接用「Trimix」一詞帶過、沒有「混合氣體」這類冗餘修飾語，不存在同樣問題，故不需要比照修改，只有 zh-Hant/zh-Hans 兩個中文變體受影響。
3. **"Visibility"（id/ms 印尼文/馬來文）**：使用者提供專門委託的 `Translation_Review_Report.md`（校對重點含 id/ms 查證），裁定採用該報告建議 → id「Jarak pandang」／ms「Jarak penglihatan」（理由：PADI 教材與當地導潛慣用「視線距離」表達能見度，"Visibilitas" 是生硬音譯）。

**該報告另外提出 6 項 id/ms 建議，一併評估後全數採用**（同批套用，2026-07-26）：
- **Weight**（id/ms）：「Berat」→「Pemberat」——**已用 App 內既有字串驗證**：「Weight Total」「Weight: %.1f kilograms」兩個既有 key 的 id/ms 翻譯本來就已經用「Pemberat」，原本「Weight」單獨用「Berat」是既有的內部不一致，此次修正後統一。
- **Ascent Rate Alert**（id/ms）：「Peringatan Laju Ascent」/「Amaran Kadar Ascent」→「Peringatan Kecepatan Naik」/「Amaran Kadar Naik」——**已用 App 內既有字串驗證**：同一支 App 的「Safety stop became mandatory...」句子已用「kecepatan naik」(id)／「kadar naik」(ms) 翻譯上升速度，原字串把「Ascent」原文直接混雜進印尼文/馬來文是既有的內部不一致，此次修正後統一。
- **Wetsuit**（id/ms）：「Pakaian Menyelam」→「Wetsuit」——**已用 App 內既有字串驗證**：「Wetsuit Thickness」的 id/ms 翻譯本來就已經用「wetsuit」一詞（`Ketebalan wetsuit`），此次統一沿用英文專有名詞，不再用冗長的「Pakaian Menyelam」。
- **Ceiling**（id/ms）：「Plafon dekompresi」/「Siling deko」→「Batas dekompresi」/「Had deko」——採用報告建議，理由是「Plafon」「Siling」在印尼文/馬來文字面上專指建築物天花板（false friend，非潛水減壓上限語境），App 內無既有對照字串可交叉驗證，屬於信任報告專業判斷的套用，非內部一致性修正。
- **Cylinder Material**（id/ms）：「Material silinder」/「Bahan silinder」→「Bahan tabung」/「Bahan tangki」——採用報告建議（當地潛水圈慣用 tabung(id)/tangki(ms) 稱氣瓶，非 silinder），同樣屬信任報告專業判斷，App 內無既有對照可驗證。
- **Cylinder Size**（id/ms）：「Ukuran silinder」→「Ukuran tabung」(id)／「Saiz tangki」(ms)——理由同上，信任報告專業判斷套用。

**⚠️ 提醒**：後 3 項（Ceiling／Cylinder Material／Cylinder Size）屬於「信任報告專業判斷」而非「App 內部一致性驗證」，与前 3 項（Weight／Ascent Rate Alert／Wetsuit）的驗證強度不同——若日後有印尼文/馬來文母語潛水員可覆核，建議優先看這 3 個 key。

以上共 18 個儲存格套用，`git diff --stat` 確認改動範圍精確對應（18 行變更），iOS + macOS build 皆通過。

**2026-07-26 事後複查（使用者主動要求「再做一遍翻譯品質審核」）**：套用完 18 個儲存格後，沒有直接收尾，改用程式掃描全檔案找「這次改的詞，有沒有姊妹 key 忘記一起改」，抓出 **3 個真的漏改的姊妹 key**（都是同一批 18 個儲存格套用時，只改了 report 明確點名的 key，沒有連帶檢查同語意但不同 key 的其他出現位置）：
- `Max Ceiling`（id/ms）：跟 `Ceiling` 同樣的「Plafon/Siling」false friend 問題，`Ceiling` 已修但 `Max Ceiling` 漏了 → 改為 `Batas maks.`(id)／`Had maks.`(ms)，跟 `Ceiling` 的新譯法一致。
- `Ascent rate exceeded 10 m/min (32.8 ft/min).`（僅 id）：跟 `Ascent Rate Alert` 同樣的「Ascent 原文直接混雜進印尼文」問題，`Ascent Rate Alert` 已修但這句內文漏了 → `Laju ascent melebihi...` 改為 `Laju naik melebihi...`。（ms 版本 `Kadar menaik melebihi...` 本身沒有這個問題，不需要改。）
- `Visibility: %.1f metres` / `Visibility: %@` / `Visibility: Not recorded`（id/ms，共 6 個儲存格）：跟裸 key `Visibility` 同樣用「Visibilitas」，這 3 個帶前綴/格式參數的姊妹 key 完全沒被檢查到，一樣改為 `Jarak pandang:`(id)／`Jarak penglihatan:`(ms) 開頭，格式參數（`%.1f m`／`%@`）位置與數量已用腳本核對與 en 版一致。

修完後再次全檔案掃描確認：id/ms 已無殘留的 `silinder`／`Ascent`／`Plafon`／`Siling`／`Visibilitas`／`Pakaian Menyelam`；格式參數驗證器（同先前用於 rev05 損毀修復的邏輯）掃過全部 260 個 key，0 個問題。iOS + macOS build 再次皆通過。**累計本輪（#16 尾聲）共套用 27 個儲存格**（18 + 9），`git diff --stat` 確認為 27 行變更、無額外異動。**教訓記入 HANDOFF「陷阱提醒」**：改術語時要連帶 grep 同一詞在其他 key 的出現位置，不能只改報告明確點名的 key。

（其餘 462 筆審核建議中，432 筆已被 rev05 涵蓋、剩下的多為審核者自己資料損毀或建議本身有誤——如 ppl 對 `%lld dive%@ imported` 跟 `kg`(it) 的建議都是格式錯誤或誤存編輯備註，已核實駁回不採用，不影響上述項目）

| 17 | 真機回報：地圖/剖面圖語系殘留＋剖面圖英制單位失效＋全專案 String(localized:) 掃除＋DiveImportKit 錯誤訊息修復未落地 | ✅ 完成（翻譯內容縮短留給後續 AI 校閱決定，非 bug） | 使用者截圖回報 4 個現象，逐一查根因：**① 語言切換後地圖/日誌詳情殘留切換前語言**（截圖：日文切繁中後「ダイビング情報」「ガス」仍是日文）——根因確認為 `String(localized:)` vs `languageManager.localized(_:)` 這個舊病（v1.1 #8／v1.2 #7 修過 Import tab，但沒有全專案掃過）：`String(localized:)` 讀系統 `Locale.current`，`AppLanguageManager` 切語言時只有「下次啟動」才會反映到系統層（見 `AppLanguageManager.swift` 檔頭「四段式解法」註解），當下不重開 App 就會殘留舊語言；`Text(LocalizedStringKey)`／`DiveStatCell(label:)` 因為吃 root 的 `.environment(\.locale)` 才會即時跟隨，這也是同一畫面部分文字（「最大深度」等）正常、部分文字（「ダイビング情報」等）卡住的原因。**已修復**：`DiveSiteSheetView.swift`（15 處全改）、`DiveRowView.swift`（2 處）、共用的 `JD2Core/Utilities/GasMix+LocalizedDisplay.swift`（改吃 `AppLanguageManager` 參數，因為是 `GasMix` extension 不是 View，沒有 SwiftUI Environment 可用，呼叫端 `DiveSiteSheetView`/`DiveRowView`/`DiveLogDetailView` 三處呼叫點同步更新)。**2026-07-26 已完成全專案掃除**：`SettingsView.swift`(13)／`DiveLogEditSheet.swift`(35)／`DiveLogDetailView.swift`(31)／`DiveCalendarView.swift`(5)／`MapView.swift`(7)／`DiveAnalysisView.swift`(4)／`DiveLogListView.swift`(1)／`MainTabView.swift`(4)／`LogbookContainerView.swift`(3)／`DiveMapRepresentable.swift`(1)／`DiveSiteAnnotation.swift`(1) 全部改用 `languageManager.localized(_:)`。逐檔核對後全部改對：沒有語言環境可用的非 View 型別（`DiveSiteAnnotation` 是純 `NSObject`/`MKAnnotation`）改成建構子吃呼叫端（`DiveMapRepresentable`）已解析好的字串，不在型別內部呼叫任何 localize API；`GasMix+LocalizedDisplay.swift`（`GasMix` extension，同樣沒有 Environment）改吃 `AppLanguageManager` 參數。**兩個確認不是 bug、刻意不動的地方**：`DiveCalendarView.swift` 的 `.accessibilityLabel("Previous month")`（純字面量，Swift 會選 `LocalizedStringKey` overload，本來就跟著 `.environment(\.locale)` 走，跟其餘 `String(localized:)` 包一層的寫法不同，不需要改）；`DiveAnalysisView.swift` 的 `warningTitle`/`warningDetail`（被 `showWarningEvents=false` 藏住的既有已知技術債，見 v1.2 #3 註解，這次一併修正避免功能重開時繼承舊 bug）。全專案 `grep -rn "String(localized:" JD2-Logbook JD2Core --include="*.swift"` 只剩註解裡的說明文字，實際呼叫 0 處。**額外發現並修復一個相關但獨立的 bug**：查驗希臘文截圖裡「不支援的格式:」「解析失敗:」仍是中文（明明 `_JD2-family/reports/R-2026-07-25-DiveImportKit錯誤訊息未本地化.md` 已記錄家族總指揮 2026-07-25 把 Kit 的樣板文字改成英文），追查發現 `JD2Core/Importers/DiveLogImporter.swift` 有一份**本地複製的同名 `DiveLogImportError` enum**（`DiveImportKitAdapter.mapImportKitError` 把 Kit 的錯誤逐 case 轉成這個本地型別，該 adapter 自己註解明講「訊息內容與 Kit errorDescription 一致」），Kit 端 2026-07-25 那次修復沒有連帶檢查到這個本地複製版，導致 Kit 已經修好、但實際顯示的錯誤訊息仍走本地複製版的舊中文樣板——不是「Kit 修復沒生效」，是兩份重複定義各自為政。已同步把本地 enum 的 6 個樣板文字改成跟 Kit 一致的英文，不動 Kit 本身（已經是對的，不需要回報家族層）。已記錄到 `SYNC_TO_JD2-ULTRA.md` #8，提醒 Ultra 若之後採用同款 adapter 模式要注意這個同步陷阱。以上全部改動已跑過 iOS + macOS build 確認皆通過，非本次範圍的部分（v1.2 #3 warning 功能是否重新開放、`DiveImportKit` 34 個解析器層 detail 字串完整本地化）維持原狀不動。**② 剖面圖互動列 label 折行/截斷**（截圖：德文 "Deco-Ceiling / Decotiefe" 折成兩行、"Wassertemp." 截斷）——根因是**版面 bug ＋ 翻譯內容雙重疊加**：`calloutCell` 的 label Text 從未設過 `.lineLimit`，過長文字會折行撐高整列；同時 `Ceiling`(de: 25 字／nl: 21 字)、`No Deco`(ms/id: 26 字／el: 27 字) 這幾個語言的翻譯是「英文技術詞＋當地語」複合字串，比其他語言（中文 4 字、英文本身 7 字）長數倍，在 5 欄等寬、`.footnote` 不縮放的窄欄位裡必定爆版。**已修版面部分**：label 加 `.lineLimit(1)`，過長時用省略號截斷、不再折行撐高列高，維持跟數值列對齊。**翻譯內容是否要縮短未動**（留給使用者接下來要找的 AI 校閱一起判斷，避免我在多個語言上自行決定用詞取捨）。**③ Tissue Loading「Fast/Slow」中文不夠精確**——使用者指出對應中文應為「快組織/慢組織」，位置實際在 `DiveAnalysisView.swift:346/349`（非使用者原猜測的 91/201 行，那是舊版行號或別的檔案）；查證這兩個 key 全專案只有這一處呼叫，無副作用。已套用 zh-Hant「快組織」/zh-Hans「快组织」、zh-Hant「慢組織」/zh-Hans「慢组织」。**已核對其餘 16 語言**：全部都是跟英文 "Fast"/"Slow" 對應的裸形容詞（法文 Rapide/Lent、日文 速い/遅い 等），沒有中文這種「單獨看容易讀成純速度形容詞」的歧義問題，判斷不需要跟著加「組織」類的限定詞，維持原譯。**④ 英制單位下剖面圖 Y 軸仍顯示 m**——查證 `DiveKitUI.DiveProfileChartView`（家族共用元件）本身早就支援 `depthAxisLabel` 注入格式（doc comment 明講「呼叫端可傳入自己的單位換算格式」），純粹是 Logbook 這邊的薄包裝 `Views/Logbook/DiveProfileChartView.swift` 從未傳這個參數，落回套件預設值固定 `"...m"`——**純 App 層漏接，非 Kit 本身限制，不需回報家族層**。已修：注入 `@AppStorage(UnitSystem.storageKey)` 讀值，Y 軸刻度改用 `unitSystem.convertDepth(...)` + `unitSystem.depthSymbol` 動態格式化。以上全部改動 iOS + macOS build 皆已驗證通過，**尚未 commit**，等 PM 確認 |

| 18 | AI 稽核報告（`code_audit_report-0726.md`）逐項查證＋修復 | ✅ 完成 | 使用者提供一份第三方 AI 稽核報告（3 項「重大風險」＋3 項「優化建議」），要求逐項查證是否真實。**未照單全收，逐項讀原始碼驗證**：① `depthFormatted`（Extensions.swift，宣稱「隱藏 40m 以上深度」的重大風險）——查證全專案 0 呼叫端，純死碼，不影響任何畫面，報告誇大成「重大風險」不成立。② `DiveLogEditSheet` trimix 存檔靜默降級成 Air——**查證屬實且比報告描述的更嚴重**：`buildGasMixJSON()` 根本沒有 `.trimix` case，`save()` 無條件覆寫 `dive.gasMixJSON`，只要對任何 trimix 潛水按 Edit 再 Save（哪怕只改備註），氣體資料就不可逆遺失。③ Buhlmann `ceiling(at:)` 收到負深度的「邊界隱患」——查證 `absolutePressure()` 只是線性公式（`surfacePressureBar + depth/metersPerBar`），`ceiling()` 結果本身有 `max(0, ...)` 夾住，追蹤呼叫鏈到 `currentGF()` 也只有 `max/min` 夾住，沒有 log/sqrt/除以零等會產生 NaN/Inf 的運算，報告宣稱的「浮點數異常或計算錯誤」不成立，但兩處呼叫端（`ceiling`/`ndlSeconds`）夾負深度的方式不一致，屬合理但非急迫的一致性小修。④ `averageAscentRate` 命名與公式不符——查證屬實但全專案 0 呼叫端，純死碼，不影響任何真實上升速率警示邏輯（那是 `DiveReplayEngine` 另一套獨立實作）。⑤ `profileSamples` 每次讀取重新解碼 JSON——查證只有 1 個呼叫端（`DiveLogDetailView.swift`，解碼一次存進 `let`），報告宣稱的「列表滾動時反覆解碼」情境不存在，不是真正的效能熱點。⑥ `DiveLogEditSheet` 配重/氣瓶壓力缺英制雙向轉換——查證屬實，`weightTotal`/`cylinderStartPressure`/`cylinderEndPressure` 硬編碼 kg/bar，v1.2 #4 的單位系統工作漏了這三個欄位。**使用者確認後執行的修復**：(a) Trimix bug——新增 `originalTrimixGasMixJSON` 記錄原始 JSON，`save()` 改為 trimix 時維持原樣不覆寫；Gas picker 對 trimix 潛水加 `.disabled()`＋新增 footer 提示文字（新 key，英文 stopgap）避免使用者誤以為切換 picker 有效卻被靜默忽略。(b) 英制單位缺口——`UnitSystem` 新增 `weightSymbol`/`pressureSymbol`/`convertWeight`/`convertPressure`/`kgValue(fromDisplay:)`/`barValue(fromDisplay:)`（1 kg=2.20462 lbs，1 bar=14.5038 psi，比照既有 depth/temperature 換算風格）；`DiveLogEditSheet` 新增 3 個 `Binding<Double?>` 顯示層換算屬性，TextField／單位符號 Text 改吃這些，accessibility label 依單位系統選對應字串（metric 沿用既有 18 語言翻譯，imperial 新增 2 個英文 stopgap key，避免動到既有翻譯投資）。(c) 死碼清理（見下方使用者追問）——`depthFormatted`／`averageAscentRate` 之外，逐一驗證 `Extensions.swift` 全部 21 個符號後發現**整個檔案只剩 `diveTimeFormatted`（`Int` extension 版本）沒被外部呼叫**（`DiveLog.diveTimeFormatted` 是另一個同名但完全獨立的 computed property，兩者無關），確認整份 `Extensions.swift` 是 v1.0 舊架構遺留的死碼後**直接刪除整個檔案**（比照 `CLAUDE.md`「確定沒有呼叫端可以直接刪除完全，不留 // removed 註解」），連帶移除 `DiveLog.swift` 的 `averageAscentRate`。iOS + macOS build 皆通過，78 個既有單元測試全數通過（含刪除死碼後的回歸確認），未受影響。**為什麼一開始沒有直接移除/mark**：稽核當下的任務範圍是「驗證報告內容是否真實」，只查了報告點名的 2 個死碼符號（`depthFormatted`／`averageAscentRate`）本身有沒有呼叫端，沒有主動去查同一個檔案裡其他 19 個沒被報告提及的符號——這次使用者追問才做了全檔案逐一驗證，才發現整個檔案都是死碼，一次徹底清乾淨，不留殭屍檔案。**2026-07-26 使用者實測截圖回報追加修正**：新增潛水時氣瓶起始/結束壓力的硬編碼公制預設值（200 bar／50 bar）換算成英制後出現「2,901 psi」「725 psi」這種假精確度零頭數字，容易誤導使用者以為是有意義的量測值。比照同表單 `airTemperature`／`visibility` 既有的「nil = 未記錄」寫法，把這兩個欄位的預設值從 200/50 改成 `nil`，讓使用者自己輸入；連帶把 placeholder 從硬編碼的「200」/「50」改成「–」（沿用 Air Temperature 欄位同款佔位符），數值為 nil 時文字轉淡灰色（`.secondary`），並修正 Start Pressure 的 accessibility label（原本 `cylinderStartPressure ?? 0` 在 nil 時會誤報「0 bar」，改成比照 Air Temperature 的 `.map { ... } ?? "...Not recorded"` 寫法，新增 1 個英文 stopgap key）。iOS + macOS build 皆通過。**使用者接著要求全面盤點「所有牽扯到單位換算的欄位」是否有類似問題**：全專案搜尋 `unitSystem.convert*`/`unitSystem.format*` 用法，確認只有 `DiveLogEditSheet.swift` 是實際輸入表單（其餘 `DiveRowView`/`DiveLogDetailView`/`DiveSiteSheetView`/`DiveAnalysisView`/`DiveMapRepresentable`/`DiveProfileChartView` 都只是唯讀顯示既有資料，不涉及「預設值」問題）。表單內剩餘 3 個帶單位換算的硬編碼預設值，逐一處理：**`weightTotal`（0→nil）**：屬於 `Double?` optional 欄位，跟氣瓶壓力同一類問題（0 無法區分「沒填」跟「真的配重 0」，例如乾式衣配重可能真的是 0），比照同樣手法改掉，UI/a11y 處理方式一致（新增 1 個英文 stopgap key）。**`maxDepth`（18.0→0）**：查證 `DiveLog.maxDepth` 是 SwiftData model 裡的**必填** `Double`（非 optional），無法真的留白，改 Optional 需要 schema migration，範圍過大不在此次處理。改用 0 當「未填入」的哨兵值——0m 深度在潛水情境下定義上不可能發生（沒有人「潛到水面」），且表單本來就有 `isSaveEnabled = maxDepth > 0 && durationMinutes > 0` 這個既有驗證關卡，把預設值也設成 0 剛好借用這個既有機制擋掉未填就存檔，不需要新增任何驗證邏輯；同時把數值文字在 0 時轉淡灰色，視覺上讀起來像「空」而非「使用者真的填了 0」。**`waterTemperature`（維持 28.0°C）**：跟 `maxDepth` 情境不同——水溫沒有一個像「深度 0」這樣物理上不可能發生的哨兵值（0°C 是冰潛真實可能出現的水溫），要做到跟其他欄位一樣徹底不給預設值，需要額外加一個「是否已填寫」追蹤旗標並塞進 `isSaveEnabled`，這會改變新增潛水表單既有的存檔行為（多一個必填關卡）。判斷這是使用者體感取捨、不是單純的 bug 修復，提出讓使用者選：**使用者選擇維持現狀**，理由：28°C 換算成 82.4°F 沒有 bar→psi 那種假精確度零頭數字的問題，且水溫本來就沒有可借用的「不可能值」哨兵，不強制使用者一定要填。iOS + macOS build 皆通過，78 個既有單元測試全數通過（第三次驗證）|

| 19 | Xcode Issue Navigator「57 個 issue」逐項查核 | ✅ 完成（核心 bug 已修，其餘分類已決） | 使用者截圖 Xcode Issue Navigator 顯示 57 個 warning，詢問要不要處理。CLI `xcodebuild` 只會印真正的編譯器 warning（干淨 clean build 後僅 6 條，見下），String Catalog 的「unused key」「percent formatting」提示是 Xcode IDE 自己的分析，不會出現在命令列 log，只能用程式碼比對間接還原。**逐類查證**：①**2 條既有 Swift 6 並行 warning**（`ImportCoordinator.swift` `formatDisplayName`/`parseAndValidateForBackground`）——`SYNC_TO_JD2-ULTRA.md`「風險 #3 已知限制」段落已記載的舊技術債，非新問題，維持現狀。②**`plotAreaFrame` 已棄用（iOS 17 起改名 `plotFrame`）**——`DiveAnalysisView.swift` 本次會話稍早才寫的程式碼，屬實。已修：改用新 API `proxy.plotFrame`（型別是 `Anchor<CGRect>?`），用 `if let` 展開整個 chartOverlay 內容而非強制解包，避免非必要的崩潰風險。③**Percent formatting 建議**（`%lld%%`／`Nitrox O2: %d percent` 兩個 key）——Xcode 建議改用 `.percent` FormatStyle 取代手動接 `%%`，因為不同語系的百分比排版習慣不同；查證只有這 2 個 key 涉及，非功能性 bug，純風格建議，優先度低，本次不動。④**「References to this key could not be found in source code」（unused key）**——寫了一個腳本比對全部 265 個 key 是否有原始碼引用，抓到 60 個候選「孤兒 key」，但**手動逐一核對後發現腳本本身誤判率很高**：`Max Ceiling`／`Min NDL`／`Weight Total`／`%lld dive%@ imported` 等抓到的「孤兒」，有些是腳本沒抓到跨行 `String(format:)` 呼叫的 false positive，但另外幾個（`File %lld of %lld`／`%lld dive%@ imported`／`%lld skipped (duplicates)`／`Importing file %lld of %lld: %@`）**追查後發現是真的斷線**：`ImportWizardView.swift` 的匯入進度／結果畫面實際上用的是 `Text("\\(current) of \\(total)")`／`Text("\\(count) dive... imported")` 這類**純字串插值**，完全沒有走任何在地化機制，不管 App 設什麼語言永遠顯示英文——而對應的、早就有 18 語言完整翻譯的 key 就閒置在旁邊沒人呼叫。用 `git log -S` 確認這是 v1.1 port（commit `f326c90`）就存在的舊 bug，非本次會話造成的回歸。**已修復**：`ImportWizardView.swift` 4 處全部改回呼叫既有 key（`languageManager.localized(...)` + `String(format:)`），不需要新翻譯，直接把閒置的既有翻譯接回去。⑤**發現一個尚未決定如何處理的東西**：追查 `Max Ceiling`／`Entered Deco`／`Decompression Analysis` 這幾個 key 為什麼真的沒人呼叫時，連帶發現 `DiveReplayEngine.ReplayResult.maxCeiling`／`.enteredDeco` 這兩個計算屬性本身也是 0 呼叫端——`git log -S` 確認三者都是同一個 v1.1 port commit（`f326c90`，那次一次新增 192 行 `DiveAnalysisView.swift`）引入、之後從未被任何 commit 動過，第一時間判斷是演算法層跟翻譯 key 都準備好但沒接 UI 的殘留，問使用者要不要清掉，使用者選「清掉」。**但動手前重新核對時發現自己重複了同一種疏忽**：`grep` 範圍只查了 `JD2-Logbook JD2Core`，沒查 `JD2-LogbookTests`——`DiveReplayEngineTests.swift` 其實有 5 個斷言在用 `maxCeiling`／`enteredDeco`／`finalTissuePressures`，**真實驗證「潛水是否進入減壓」「最深天花板深度」「16 隔室最終氮分壓數量」的演算法邏輯**，不是死碼。已回頭跟使用者更正，重新確認後**使用者決定全部保留不動**（含 3 個對應翻譯 key），不當作待清理項目。**教訓**：跟先前腳本抓「孤兒 key」誤判的同一個根因——查「有沒有被呼叫」範圍要涵蓋測試目標，不能只查 App/Core 原始碼，這次連續踩了兩次才確認乾淨，記錄下來提醒下次直接把測試目錄一起納入初始查詢範圍，不要事後才補查。**其餘 ~50 個未逐一核對的候選孤兒 key**：鑑於腳本誤判率已證實偏高、且至少 4 個「看起來像孤兒」的其實是真正的斷線 bug 而非死碼，不建議再用同一支腳本批次刪除，之後如果要處理建議一次只查一兩個、或直接在 Xcode 開專案用「Fix」逐一確認再決定。iOS + macOS build 皆通過 |

| 20 | 真機截圖回報：`DiveLogDetailView` 配重/氣瓶壓力英制未換算 | ✅ 完成 | 使用者截圖：英制設定下「能見度」正確顯示 9.8 ft（換算生效），但「配重」仍顯示 5.4 kg（單位符號跟數字都沒變）。查證 `DiveLogDetailView.swift`：`Weight`（168 行）／`Start Pressure`（177 行）／`End Pressure`（180 行）三處都是獨立寫死的 `String(format: "%.1f kg", ...)`／`String(format: "%.0f bar", ...)`，完全沒有呼叫 `unitSystem`——跟 v1.2 #18 修的 `DiveLogEditSheet`（可編輯表單）是同一個「配重/氣瓶壓力缺英制換算」問題，但發生在**另一個獨立檔案**（唯讀詳情頁），兩處各自寫死、互不相關，修一邊不會影響另一邊。**這是這次會話 #18 做「全面盤點單位換算欄位」時的漏網之魚**：當時的 grep 只確認 `DiveLogDetailView.swift`有沒有用到 `unitSystem.convert*/format*`（有，因為 maxDepth/waterTemperature/visibility 已經在用），但沒有反過來確認同一個檔案裡「本來就該用但沒用」的欄位——只查「這個檔案有沒有用到」，沒查「這個檔案裡的每個相關欄位是否都有用到」，兩者是不同的查證粒度，這次才補上。已修復：比照 `DiveLogEditSheet` 同樣的 `unitSystem.convertWeight`/`unitSystem.weightSymbol`／`unitSystem.convertPressure`/`unitSystem.pressureSymbol` 呼叫方式套用到這三處；全專案搜尋確認沒有第三個地方也寫死 kg/bar（`DiveRowView`／`DiveSiteSheetView`／`DiveAnalysisView` 皆無 `weightTotal`/`cylinderStartPressure`/`cylinderEndPressure` 引用，範圍僅限這兩個檔案）。iOS + macOS build 皆通過 |

| 21 | 第二輪 AI 稽核報告（`audit_report-0726-R2.md`）逐項查核 | 🔄 1 項已修（R-03），其餘記錄待決 | 使用者提供第二份第三方 AI 稽核報告（聲稱「4 名子代理平行讀取全部 37 個原始碼檔案」），12 項風險＋26 項建議。**開場就抓到報告本身有嚴重事實錯誤**：聲稱 App 只支援「6 種語言（en/zh-Hant/ja/ko/es/fr）」，但 `AppLanguageManager.supportedLanguages` 實際是 18 種——這個錯誤直接連帶讓報告裡好幾項「未本地化」的指控站不住腳。**逐項查證，分四類**：\n\n**A. 確認為真、已修復**：R-03 `DiveLogListView.StatsHeaderView.deepestDive` 寫死 `String(format: \"%.1fm\", ...)`，跟今天已經修過 3 次的「配重/氣瓶壓力缺英制換算」是同一類 bug 的第 4 個實例，且是這次會話目前為止唯一沒查過的檔案。已修：改用 `unitSystem.formatDepth(...)`，並全專案搜尋確認沒有第 5 個地方也寫死 `%.1fm`/`%.0fm`。iOS + macOS build 皆通過，78 測試全過。\n\n**B. 確認為真、但屬於死碼（優先度低）**：R-04（`DiveLogDatabase.getStatistics().averageDepth` 語義錯誤，實際算的是「各潛水最大深度的平均」不是「平均深度」）——查證 `getStatistics()`／`DiveStatistics` 全專案 0 呼叫端（含測試），跟今天稍早清掉的 `depthFormatted`／`averageAscentRate` 同一類：問題敘述正確，但目前完全不會顯示在畫面上，非緊急。\n\n**C. 確認為真、需要使用者決定範圍（非我能單方面決定的判斷/風險）**：\n- R-01（`NSAllowsArbitraryLoads=true` 關閉 ATS）——查 git log 確認**這是刻意加的**（commit 訊息明講「AdMob 真機廣告必要條件」），不是疏忽；報告用「除非有明確理由否則應移除」的假設性框架來寫，但理由其實已經寫在 commit 歷史裡。是否要嘗試移除、真機重測廣告是否還能載入，這是需要使用者決定+配合真機測試的事，不擅自刪。\n- R-06（`NSLocationWhenInUseUsageDescription` 只有英文，沒有各語言 `InfoPlist.strings`）——查證屬實，全專案沒有任何 `InfoPlist.strings` 檔案。這是系統權限彈窗的本地化，跟 App 內 `Localizable.xcstrings` 是不同機制，目前 18 語言都只顯示這段英文。\n- R-07（SwiftData 無 `MigrationPlan`）——報告貼的程式碼片段（`.modelContainer(for: DiveLog.self)`）跟實際不符（實際是 `DiveLogDatabase.swift` 自建 `Schema`＋`ModelContainer`），但底層論點站得住腳：確認目前只有單一版本 `Schema([DiveLog.self])`，沒有 `SchemaMigrationPlan`。未來若 `DiveLog` 有 breaking schema 變更，沒有遷移路徑。\n- R-12（無 GDPR/CCPA 廣告同意機制／UMP SDK）——查證屬實，`GoogleUserMessagingPlatform` package 有被解析成相依套件但完全沒有程式碼呼叫任何 UMP API。對照 `V1_2_BACKLOG.md` #1 記錄的送審因應措施（AdMob 預設非個人化廣告），團隊當時選的是「非個人化廣告」這條路而非完整 UMP 同意流程，這是法遵層級的產品決策，不是我能單方面決定要不要補的事。\n\n**D. 查證後發現與現況不符或直接是編造**：\n- R-05（宣稱 `DiveAnalysisView`/`ImportWizardView`/`MapView`/`DiveSiteSheetView`/`SettingsView` 等一大批字串「未本地化」）——**逐一查證全部是假的**：報告列的每一個範例字串（\"Tissue Loading\"／\"Time\"／\"Depth\"／\"Ceiling\"／\"Try Again\"／\"Dive Site\" 等）在 `Localizable.xcstrings` 裡都有完整 18 語言翻譯，且透過 SwiftUI `Text(LocalizedStringKey)` 的自動查表機制正確跟著 `.environment(\\.locale)` 走——這正是本次會話稍早就已經驗證過的機制（純字面量 `Text(\"X\")` 不需要额外包 `languageManager.localized()` 也會正確運作）。報告顯然不理解這個機制，把「沒有用 `languageManager.localized()` 包」誤判為「沒有本地化」。\n- S-06（宣稱 `MapView`/`DiveLogDetailView` 的座標過濾邏輯一個用 OR 一個用 AND、建議改用 Optional 取代 0.0 哨兵值）——查證兩處目前都是一致的 `latitude != nil && longitude != nil`／`guard let lat, let lon`，本來就已經是 Optional-based 寫法，報告建議的修法其實就是現狀，指控的不一致並不存在。\n- S-11（宣稱 `DiveRowView` 有 `.frame(width: 4, height: max(20, CGFloat(diveLog.maxDepth) * 0.8))` 這行深度指示條寫死公制）——**逐字搜尋整個檔案找不到這行程式碼**，且用的變數名稱 `diveLog` 跟本專案一貫用的 `dive` 命名慣例不符，判斷是報告憑空生成、不是真的讀到這個檔案的內容。\n\n**未逐項查證的部分**：R-02（trimix 短路保護，跟本次會話已充分驗證過的行為一致，非新資訊）；R-08～R-11（備份/資料安全/DiveKit 演算法建議，屬架構層次的合理建議但非「發現一個 bug」，且 R-11 建議「獨立稽核 DiveKit」按家族鐵律屬於要回報家族層的事，不是 Logbook 單方面能做的）；S-01～S-26 中除了上面提到的幾項，其餘多是架構品味/測試覆蓋率建議（MVVM 重構、State 管理一致性、Accessibility 補強等），不是可以簡單「對/錯」查證的具體 bug，且部分（如 S-01 全面 MVVM 重構）改動範圍大，屬於需要使用者決定是否要投入的獨立工作項，未在本次逐一查證或執行。iOS + macOS build 皆通過 |

| 22 | 真機回報：泰文 "No Deco" 顯示截斷成英文 | ✅ 完成 | 使用者截圖：泰文語系下互動剖面圖的「No Deco」欄位顯示「No Deco...」（英文＋省略號，不是泰文）。查證發現 v1.2 #17 那次的「Ceiling/No Deco 雙語複合字串過長」問題**只查了一部分語言**（當時只抓到 de/nl 的 Ceiling、ms/id/el 的 No Deco），這次用程式全面掃過全部 18 語言 × 2 個 key，找到**額外 4 個先前完全沒查到的漏網之魚**：法文 No Deco（28 字，"Sans décompression / No deco"）、越南文（20 字）、泰文（20 字，這次使用者截圖抓到的）、克羅埃西亞文（26 字）——全部都是同一種「英文詞＋當地語」複合字串模式。**這次做兩件事**：① 版面結構修正——`calloutCell` 的 label 除了原本的 `.lineLimit(1)`，加上 `.minimumScaleFactor(0.65)`：這是安全相關資訊（免減壓時間標籤），單純用省略號截斷等於讓使用者看不懂內容，比縮小字級更糟，改成優先縮小字級塞進去，真的縮到底還是塞不下才觸發省略號當最後防線。這是跟語言內容無關的通用修法，一次解決所有語言（含未來新增語言）的潛在超長問題。② 內容修正（安全、零猜測的機械式修法）——對 9 個確認是「英文/當地語」複合字串的 key（Ceiling 的 de/nl；No Deco 的 fr/id/ms/vi/th/el/hr），**直接刪掉冗餘的英文那一半，保留當地語言那一半**（不是重新翻譯或改寫語意，純粹移除已經在原字串裡的重複英文詞），例如泰文「No Deco / ไม่มีดีโก้」→「ไม่มีดีโก้」、法文「Sans décompression / No deco」→「Sans décompression」。**還剩 6 個超過 12 字但已經不是雙語複合字串、需要真的改寫縮短才能再短的**（es Ceiling 22 字「Techo de descompresión」、id Ceiling 16 字、fr/id/ms/el/hr No Deco 16～18 字）——這些沒有「刪掉重複英文」這種零風險做法可用，縮短需要對當地語言的實際判斷，跟先前決定一致：不在低信心語言上自行發揮，留給後續語系校閱，靠新加的 `.minimumScaleFactor` 保底（會縮小字級但完整顯示，不會再被省略號截斷）。iOS + macOS build 皆通過 |

| 23 | 真機回報：德文 "Wasserte..." 截斷，追加徹底掃除 calloutRow 全部 5 欄長度問題 | ✅ 完成（僅剩 2 個無precedent 可用、需母語審核） | 使用者德文截圖出現「Wasserte...」截斷（同時看到的「Deco-Ce...」經確認是**尚未重裝新版**的舊 build，非新問題），並質問「為什麼這個問題一直清不乾淨」。**承認先前 v1.2 #22 的排查方法有缺陷**：用「>12 字元」門檻篩選，但德文「Wassertemp.」是 11 字元，剛好躲過門檻卻仍在窄欄位裡截斷——門檻本身訂得不合理，且字元數比較法對非拉丁字母（泰文等）也不準確（同樣字元數的泰文視覺寬度可能明顯更寬）。**這次改成不設門檻，直接列出全部 5 個 key（Time/Depth/Temp/Ceiling/No Deco）× 18 語言＝90 格逐一檢視**，依實際長度排序找出所有偏長項目。**修復**：① 德文 Temp「Wassertemp.」→「Temp.」（比照 fr/es/it/nl/pt-PT/hr 六個語言都用裸詞「Temp.」的既有慣例，德文沒必要多掛「Wasser」前綴）。② 找到 5 個**有同檔案內既有先例可循、非憑空發明**的縮短案例：西班牙文 Ceiling「Techo de descompresión」→「Techo」（比照葡萄牙文已驗收的裸詞「Teto」，同源詞）；印尼文 Ceiling「Batas dekompresi」→「Batas deko」（比照馬來文已驗收的「Had deko」短形式）；法文 No Deco「Sans décompression」→「Sans déco」（比照西/葡/義三個語言已驗收的「[without]+deco」模式：Sin deco/Sem deco/No deco）；印尼文/馬來文 No Deco「Tanpa dekompresi」→「Tanpa deko」（比照馬來文 Ceiling 自己的「deko」短形式）。③ 版面結構再加強：`calloutCell` 的 label 除了 v1.2 #22 加的 `.lineLimit(1)`，本次會話已經有 `.minimumScaleFactor(0.65)` 兜底，這次驗證這個 fallback 機制對縮短後的殘留長字串（多半 ≤12 字）已經足夠，不需要再改。**最終只剩 2 個真的沒有同檔案內既有先例可用、需要母語審核才能安全再縮短的**：希臘文 No Deco「Χωρίς αποσυμπίεση」（17 字，希臘文全檔案沒有「deco」外來語簡稱先例）、克羅埃西亞文 No Deco「Bez dekompresije」（16 字，同樣沒有先例）——這 2 個不猜測硬改，留給母語審核，`.minimumScaleFactor` 兜底確保至少會縮小字級完整顯示、不會被省略號截斷成看不懂的內容。iOS + macOS build 皆通過 |

## 使用方式

- 每項工作展開前，先跟使用者確認細節範圍（尤其 #1 需要等使用者提供 Apple 拒絕信完整內容）。
- 完成一項，狀態改為 ✅ 完成，並在「細節」欄補上實作摘要／commit 參照，比照 `V1_1_BACKLOG.md` 的紀錄方式。
- 若某項工作展開後發現屬於家族層共用（DiveKit／DiveImportKit）範圍，依 `../CLAUDE.md` 家族鐵律第 8 條「回報鏈」處理，不自行跨 repo 修改。
- **不手動維護檔案頂／尾的版本號與日期戳**（2026-07-25 起，比照 `CLAUDE.md`「最新 commit：以 `git log` 為準」的既有慣例）：這份文件每次都是單獨改某一列的狀態/內容（`Edit` 工具的 old_string→new_string 是逐行操作，不會自然帶到改頭尾），改了十幾次表格內容卻沒同步頭尾日期是這份文件的已知慣犯模式。與其每次都要記得多做一步、又每次都會忘，不如直接讓「最後更新」這個資訊改指向 `git log -- V1_2_BACKLOG.md`（一定準確、不可能忘記更新），從根源消除這個問題，而不是靠自律。

---

**建立日期：** 2026-07-20 | **最後更新：** 見 `git log -- V1_2_BACKLOG.md`（本檔不再手動維護版本號/日期戳，理由與作法見上方「使用方式」）
