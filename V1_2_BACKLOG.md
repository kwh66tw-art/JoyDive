# JD2-Logbook v1.2 Backlog（待辦追蹤）

> 建立日期：2026-07-20。v1.1 backlog（`V1_1_BACKLOG.md`）已完工，本檔案是下一輪工作的
> 追蹤清單，目前為**初始登錄**，多數項目尚待使用者補充細節後才能展開執行。
> **單一權威來源**：本輪工作以此檔為準，`Docs/KNOWN_ISSUES.md` 只放精簡清單＋連結於此。

---

## 待辦清單

| # | 項目 | 狀態 | 細節 |
|---|------|------|------|
| 1 | iOS 版送審被拒絕，擬訂下次送審對策 | 🔄 進行中 | 收到完整拒絕內容：2.3.6（Advertising 未勾選，Age Rating 需改 Yes，純 ASC 設定，使用者需自行至 App Store Connect 修改）+ 5.1.2(i)（App Privacy 標示 Coarse Location/Other Usage Data/Device ID 用於追蹤，但程式碼查核確認**未使用 ATT、未用 IDFA、AdMob 為預設非個人化廣告、CoreLocation 僅本機一次性定位**，判定為 ASC 隱私標籤誤填，非真的有追蹤行為）。已修復 `logbook/privacy.md` 三語（EN/繁中/日文）內文，移除「IDFA 僅在授權後」「ATT 重設」等與實際行為不符的敘述。**尚待使用者執行（送審時提醒）**：① ASC Age Rating 勾選 Advertising=Yes；② ASC App Privacy 問卷重新填寫，Location/Device ID/Usage Data 取消勾選「used to track」；③ 送審 Review Notes 註明已修正。**⚠️ 使用者已指示：處理完成後（送審過關/事件落幕時）要把本次駁回事件（原因/分析/對策/行動/lessons learned）整理記錄到適當檔案，目前尚未執行，屆時提醒** |
| 1b | 「開啟 Game Center」誤設定（iOS + macOS 皆受影響） | ✅ 已修復（程式碼問題） | 查證：程式碼內完全沒有 GameKit/GameCenter API 或 entitlement，根因是共用的單一 `Info.plist`（iOS/macOS 雙平台共用）內 `LSApplicationCategoryType = public.app-category.sports-games`——這是 Apple 官方「Games → Sports」子分類，會讓系統把本 App 當成遊戲，自動觸發 iOS 18+/macOS 的 Game Mode／Game Center 相關行為（使用者截圖證實 iOS 端也出現「遊戲模式：已開啟」橫幅，非原先以為的 macOS-only）。已改為 `public.app-category.healthcare-fitness`（無遊戲關聯的正確分類），iOS + macOS build 皆通過。**⚠️ 待使用者於下次送審時同步處理**：App Store Connect 的「App Information → Category」欄位需手動改成對應分類（Health & Fitness 或合適的對應項），確保與 Info.plist 一致，否則審核時繼續維持舊分類 |
| 1c | 匯入「File X of N」進度與「N 個 dives imported」數字不一致 | ✅ 完成（補充說明文字，非改動計數邏輯） | 使用者回報樣本檔匯入顯示 22 個檔案，匯入完後卻顯示 24 dives。核對 `ImportWizardView.swift` 的 `Text("File \(current) of \(total)")`，`total` 是**檔案數**；`ImportCoordinator.swift` 的 `successCount += result.dives.count` 則是**潛水筆數**——部分格式（如 Suunto DM5/SML XML、Diving Log 6.0 `.db`）單一檔案可能是整本「潛水日誌匯出」，內含多筆潛水，屬預期行為，非 bug。**採納使用者建議**，但保留原有已在 18 語言完整翻譯的「File X of N」字串不動（避免捨棄既有翻譯），改為在其下方新增一行說明文字「Some files may contain multiple dives.」（新 key，暫僅英文，其餘語言待 #6 語系全審核補齊），提前告知使用者數字可能不對等，避免誤以為 bug。iOS + macOS build 通過 |
| 1d | Trimix 潛水無法互動顯示組織艙殘氮（tissue saturation） | ✅ 完成（狀態列恢復互動，只隱藏真的沒有的資料；根本限制仍屬 DiveKit 家族層範圍） | 使用者截圖的 trimix 潛水（Lake Coleridge）點擊/拖曳剖面圖後看不到任何狀態列。核對 `DiveReplayEngine.swift:65-70`：trimix 潛水的 `ReplayPoint` 其實**depth/time/waterTemp 都是有算的**（單純深度/時間/溫度重放，不需要減壓生理），只有 `ceilingDepth`/`ndlSeconds`（恆為 0）跟 `tissuePressures`（空陣列）因為 DiveKit 的 Buhlmann 引擎沒有氦氣隔室模型（`ndlSeconds(at:gasMix:)` 對 trimix 會 `assertionFailure`）才拿不到。**原本的實作誤把整列都隱藏了**（`if replay.decoDataUnavailable { 免責聲明 } else { calloutRow + tissue bars }`，等於連 Time/Depth/Temp 都被免責聲明取代），**依使用者指示修正**：`calloutRow` 固定 5 欄排版（Time/Depth/Temp/Ceiling/No Deco），不因資料缺漏而增減欄位數（維持畫面一致性）——trimix 時 Ceiling/No Deco 欄位改顯示「—」佔位，且不觸發黃色/紅色警示膠囊（避免恆為 0 的假資料被誤判成「快超過免減壓時間」）；Temp 欄同樣固定顯示，樣本沒有溫度時也用「—」佔位，不再整欄消失；組織艙長條圖（`TissueBarsView`）維持用帶 `info.circle` 圖示的說明文字取代，明確告知這是已知限制、非程式錯誤。**根本限制（氦氣組織負荷計算）依家族鐵律第 8 條（回報鏈）**：這是共用 DiveKit 演算法能力缺口，影響 3 個 App，不得在 Logbook 單方面繞道實作，需回報總指揮評估是否列入 DiveKit v2.0 roadmap。iOS + macOS build 通過 |
| 1e | 狀態資訊列文字大小忽大忽小，同裝置同設定下也會不一致 | ✅ 完成 | 使用者回報兩張截圖（trimix 潛水 vs 一般潛水）的 Time/Depth/Temp/Ceiling/No Deco 文字大小明顯不同，確認並非系統文字大小設定差異（同裝置同設定仍不一致）。核對 `calloutCell`，找到兩個各自獨立、會疊加放大問題的成因：①`Text(verbatim: value).minimumScaleFactor(0.7)`——5 欄各自依「自己的字串是否超出所分到的等寬欄位」獨立決定縮放比例，不同潛水的數值字串長度不同就可能讓同一列裡各欄縮放比例不一致；②外層 `VStack` 掛的 `.animation(.easeInOut(duration: 0.15), value: selectedIndex)` 對整個狀態列（含插入時的過渡）生效，若畫面剛好在動畫還沒跑完時被截圖，文字會停在過渡中間的縮放狀態，跟動畫結束後的最終樣式不一致。修法：①移除 `minimumScaleFactor`，改用固定不縮放的 `.footnote`（在最窄的支援機型 iPhone SE、5 欄等寬版面下仍放得下目前最長的數值字串，不需要靠縮放救援）；②`calloutRow(point)` 加 `.transaction { $0.animation = nil }`，讓狀態列一律立即以最終樣式出現，不會有動畫中間態被截到的機會。iOS + macOS build 通過；**受限本機 Xcode 環境問題（Simulator MCP 工具回報 `xcode-select` 未指向 Xcode.app，需要使用者手動跑 `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer` 才能用 attach/tap 做即時視覺驗證），這次修復是基於程式碼靜態分析、未能截圖比對修復前後畫面，待使用者執行該指令或自行在裝置上覆核** |
| 2 | App 內 icon 全盤 review | ⏸️ 延後至下一版 | 使用者指示：這版要趕 3 天內上架，icon review 非上架阻塞項目，留到下一版再處理 |
| 3 | 修正潛水剖面圖互動功能問題 | 🔄 部分完成（時間軸偏移已修復；警示標記/狀態列第二列已建置但先隱藏） | ①**時間軸偏移**——根因是 `chartOverlay` 直接拿 `proxy.position(forX:)`/`proxy.value(atX:)` 當 GeometryReader 座標用，沒扣掉 Swift Charts 左側 Y 軸刻度標籤寬度；改用 `geo[proxy.plotAreaFrame]` 校正 origin。已修復，維持生效。JD2-Ultra companion 同款 bug，已記錄 `SYNC_TO_JD2-ULTRA.md` #6。②**狀態資訊列第二列**（警示事件卡片）與③**曲線警示標示**（紅點=上升過速／橘點=強制安全停留）——`DiveReplayEngine.replay()` 的上升速度追蹤邏輯（比照統一 DiveKit `AlgorithmConstants.maxAscentRateWarn` 等門檻，非另外發明數值）與對應 UI 皆已完整實作並通過 build，但**使用者決定「先 mark 掉、UI 隱藏這 2 個功能，之後改版再決定最後怎麼呈現」**。已在 `DiveAnalysisView.swift` 加入 `private let showWarningEvents = false` feature flag（含說明註解），同時 gate 掉 `selectedWarnings` 計算與曲線 `ForEach` 標記渲染；程式碼與偵測邏輯保留，未刪除，日後只需把常數改回 `true` 即可重新開放。⚠️ 已知遺留 bug（尚未修）：`warningTitle`/`warningDetail` 用 `String(localized:)` 而非 `languageManager.localized(_:)`，不吃 App 內語言切換器，功能重新開放前需一併修正 |
| 4 | 公制／英制單位設定 | ✅ 完成 | `JD2Core/Models/UnitSystem.swift`（metric/imperial 列舉 + 雙向深度/溫度轉換函式：顯示層 `convertDepth`/`convertTemperature`/`formatDepth`/`formatTemperature`，輸入層反向 `metersValue(fromDisplay:)`/`celsiusValue(fromDisplay:)`）+ Settings 頁單位切換（符號呈現：`m / °C` vs `ft / °F`），`@AppStorage` 持久化，儲存值永遠是公制。已全面套用至：`DiveRowView`、`DiveLogDetailView`（含 `DiveStatCell` 兩處）、`DiveAnalysisView`（`calloutRow` 深度/溫度/天花板）、`DiveSiteAnnotation`/`DiveMapRepresentable`（非 SwiftUI View context，改直讀 `UserDefaults`）、`DiveSiteSheetView`（含 `keyStatsRow` 兩處 `DiveStatCell`）、`DiveLogEditSheet`（新增 4 個雙向 `Binding<Double>`/`Binding<Double?>` computed property：`maxDepthDisplay`/`waterTemperatureDisplay`/`airTemperatureDisplay`/`visibilityDisplay`，輸入欄位單位符號與 accessibility label 一併改為動態）。`Extensions.swift` 內的 `depthFormatted`/`DepthLimitCheck.displayDepth` 經查證為零呼叫端死碼，未在此次範圍內處理。新增 4 個 `%@` 格式的 accessibility label 翻譯 key（僅 en，其餘語言待 #6 語系全審核時補齊）。iOS + macOS build 皆通過 |
| 5 | 匯入情境測試 | 🔄 測試素材已備妥，待使用者實機跑一輪 | 使用者指示在 `_JD2-family/00_Import_test_scenes/` 建 5 個子資料夾放測試檔。已利用既有 `00_Import_samples/`（已驗證的 14 格式匯入樣本庫）與 `dive-log-samples/`（家族原始樣本庫）真實/已註明來源的檔案組出 5 種情境，並寫入 `00_Import_test_scenes/README.md` 說明各情境測試方式與預期結果：**ITS_01** 黃金路徑（5 種格式各 1 筆潛水，5 檔→5 筆）；**ITS_02** 單檔多筆潛水（3 個 Subsurface XML/`.ssrf`，3 檔→50 筆，直接復現先前「22 檔→24 筆」情境，數字差距拉大更好驗證，順便確認新加的「Some files may contain multiple dives.」提示文字有顯示）；**ITS_03** 批次匯入 8 種格式混合（Suunto SML/SDE、Divesoft DLF、DAN DL7、Garmin FIT、Shearwater、DivingLog SQLite、Subsurface CSV，測進度條與格式自動偵測）；**ITS_04** 重複匯入／錯誤處理（同內容改檔名測去重、截斷 XML／亂碼內容／空檔案三種壞檔測錯誤處理不中斷整批）；**ITS_05** 特殊情況（真實 trimix 雙潛水 UDDF 樣本測 v1.2 剛修好的 trimix 剖面圖顯示、ZIP 包裝 UDDF 測 `MinimalZipReader`、中文檔名、Mares Dive Organizer 測未支援格式友善拒收、不相干副檔名）。後續使用者要求檢視 `dive-log-samples/`／`divelog-mock-dataset/` 還有無可用素材，找到 5 項有價值但先前未涵蓋的，追加進 ITS_04／ITS_05（不新增資料夾，維持 5 情境架構）：Divesoft DLF **v2 header**（`DivesoftDLFParser` 程式碼明確拒收的變體，先前只有單元測試測過，這次補 E2E UI 驗證）→ITS_04；`GarminConnectJSONParser`**從未被任何資料（含 mock）驗證過**的首次測試資料→ITS_05；Deepblu（格式已於 2026-07-19 正式移除支援，測乾淨拒收無殘留邏輯）→ITS_05；Seac 品牌（另一個無解析器支援的真實樣本，補充不支援格式多樣性）→ITS_05；ATMOS Mission3 跨品牌 UDDF＋FIT（探索性測試，先前曾發生過 ATMOS 的 FIT 被誤當 Garmin FIT 收錄的教訓，這次直接驗證 `GarminDescentParser` 對非 Garmin 品牌 FIT 檔的實際行為）→ITS_05。`00_Import_test_scenes/README.md` 已同步更新各項測試方式與預期結果。使用者實測回報 3 項結果，逐一查證：**① ITS_03「10 筆潛水不是 8 筆」**——非 bug，`dive_2026-06-01.zxu`（DAN DL7）樣本內含 3 組 ZDH/ZDT（`DANDL7Parser.swift` 程式碼明確支援單檔多潛水），8 檔案−1+3=10 筆為正確結果，先前 README 的「8→8」預期算錯，已更正。**② ITS_05「zip 檔匯入失敗」**——非 bug，是測試檔案命名錯誤：`UDDFParser.canHandle()` 只認副檔名字面是不是 `.uddf`，不會對 `.zip` 副檔名做內容嗅探，ZIP 解壓邏輯本身沒問題但要求維持 `.uddf` 副檔名（同 Suunto SDE 格式的慣例）；已把測試檔案從 `.zip` 改名回 `.uddf`。**③ ITS_04 語系問題**——**真的是 bug，兩個獨立成因**：(a) App 層 `Localizable.xcstrings` 的 `"Failed Files"` key **完全沒有任何語言的翻譯值**（連英文都沒有，`locales: NONE`），導致不論 App 設什麼語言都直接顯示英文 key 本身，已補上 en／zh-Hant（其餘語言待 #6 語系全審核補齊）；(b) **更嚴重**：截圖裡「解析失敗: ...」「不支援的格式: ...」這些錯誤訊息本身，不論 App 語言為何都固定顯示繁體中文——查證後這些字串是**寫死在家族共用 `DiveImportKit`**（14 個檔案都有這個問題，非單一遺漏），完全沒有走任何翻譯機制。**依家族鐵律第 8 條，這是跨 3 個 App 的 Kit 層問題，已停下回報，不在 Logbook 單方面繞道處理**：已寫成正式報告 `_JD2-family/reports/R-2026-07-25-DiveImportKit錯誤訊息未本地化.md`（含完整 34 處程式碼位置列表、根因分析、3 種修法方向建議），並登錄進 `_JD2-family/F-00-文件登錄表.md`，供接手處理 DiveImportKit 的 session／agent 直接使用（單一戰場規則，同時間限一個 session 動 Kit）。`00_Import_test_scenes/README.md` 已同步更正 ITS_03／ITS_05 的預期結果說明。iOS + macOS build（Logbook 層修復部分）皆通過 |
| 6 | 語系全審核 | ⏸️ 排在最後（使用者確認：ASC 動作也排在這之後） | 使用者指示：3 天內上架的優先序是先把所有 coding 問題（#1/#1b/#3/#5 等）全部解決，語系審核排在最後才做；ASC 後台動作（#1 Age Rating/Privacy 問卷、#1b Category 欄位）也明確排在語系校正**之後**才處理 |
| 7 | Import tab 部分文字未跟隨 App 內語言切換 | ✅ 完成 | 使用者發現日文語系下 Import tab 仍顯示英文（「Select Files or Folder」「Supported Formats」「UNIVERSAL」等）。根因與 v1.1 #8 相同類型：`ImportWizardView.swift` 用 `String(localized:)`（純 Foundation API，只認系統語言）而非 `languageManager.localized(_:)`（吃 App 內語言切換器），只是 v1.1 #8 修復當時這批 Import 格式清單程式碼還沒寫。全面替換為 `languageManager.localized(_:)`，新增缺漏的 "Import Completed with Issues" 翻譯 key（18 語言） |
| 8 | macOS 地圖沒有 recenter 按鈕 | ⚪ 非問題（使用者確認） | 查證：`recenterButton` 程式碼本身沒有 iOS-only 限制，只受 Settings 頁「Enable GPS Location」開關（`gpsLocationEnabled`）控制，該開關未開時按鈕本來就不存在——macOS/iOS 是各自獨立的 App 安裝，UserDefaults 不共用。使用者確認是 macOS 版沒開定位開關，非程式 bug，跳過 |
| 9 | 真機驗證：AdMob 廣告 / IAP 購買流程 / Restore Purchase | 🔄 進行中（IAP 購買＋Restore 正向／反向情境皆已 PASS；AdMob 官方測試 ID 已切換，僅 Settings 頁視覺確認過） | **IAP 購買流程**：使用者截圖確認 Sandbox 購買彈窗正確顯示「Sandbox」環境標籤＋「僅供測試使用，確認此購買交易不會導致扣款」，帳號 kwh2006@gmail.com，✅ **PASS**。**Restore Purchase**（正向：已購買帳號重灌後還原）：✅ **PASS**。**AdMod 視覺驗證**：`AdBannerView.swift` 的 4 個 Ad Unit ID 皆為正式 ID，程式碼原本沒有註冊 `testDeviceIdentifiers`，真機會直接請求正式廣告，有誤觸真實廣告造成 Google 判定無效流量的風險。使用者提供 Xcode Console 印出的裝置 ID（`a10efcaff62afcacbd5dc5c884fb7f6d`），已在 `JD2_LogbookApp.swift` 的 `init()` 加入 `#if DEBUG` 包住的 `MobileAds.shared.requestConfiguration.testDeviceIdentifiers = [...]`，讓真機在 Debug build 下也拿到官方安全測試廣告，可放心逐頁（Logbook/Import/Settings）檢查版位是否正常顯示、不遮擋內容；Release/Archive 送審 build 不受影響，一律走正式廣告請求。**第一輪失敗**：使用者裝上第一組 ID 後 Settings 頁仍顯示真實廣告（非 Test Ad），核對後發現 Xcode Console 重新印出的裝置 ID（`eb621500356405187fcd4d978b8c6131`）跟第一次拿到的（`a10efcaff62afcacbd5dc5c884fb7f6d`）不一樣——判斷第一組 ID 來源可能是模擬器 session 或不同裝置，對這台真機無效。已把兩組 ID 都加進陣列（`testDeviceIdentifiers = [...]`），iOS + macOS build 皆通過。重新 build 裝上真機後 Settings 頁已確認出現「Test mode」，✅ **AdMob 測試裝置註冊 PASS**（Logbook/Import/地圖空狀態版位使用者尚未逐一回報，視為同一支 App 內共用同一組 request configuration，risk 低）。**附帶發現並修復**：使用者回報 Settings 頁「Developer Tools」的「Simulate Premium (No Ads)」開關每次啟動都自動變回 ON，干擾廣告解鎖測試。查證 `SettingsView.swift` 的 Toggle 綁定 `get: { purchaseManager.isPremium }`（真實 StoreKit 授權狀態）／`set: { purchaseManager.setDebugPremium($0) }`，而 `PurchaseManager.refreshPurchaseStatus()` 每次啟動都會用 `Transaction.currentEntitlements` 重新核對真實購買紀錄——使用者測試 IAP 時完成的真實 Sandbox 購買，讓這個帳號從此永遠有效授權，`DEBUG_isPremiumOverride` 只能強制設為 true、無法強制蓋掉真實授權設為 false，導致開關永遠被真實購買狀態覆蓋回 ON，並非隨機故障。**依使用者指示直接移除**：`SettingsView.swift` 拿掉整個 Toggle；`PurchaseManager.swift` 一併移除已無呼叫端的 `setDebugPremium()`／`DEBUG_isPremiumOverride` 相關程式碼（避免留下死碼）。iOS + macOS build 皆通過。**Restore Purchase 反向情境**：使用者在 ASC 對 `kwh2006@gmail.com` 執行 Clear Purchase History 後重灌 App，仍顯示「進階版已解鎖」，詢問是否程式碼沒設好。查證後判斷**非程式碼問題**，是 StoreKit 2 Sandbox 已知的本地快取行為：`Transaction.currentEntitlements`（`PurchaseManager.refreshPurchaseStatus()` 用來判斷購買狀態）讀的是**裝置端** StoreKit 交易快取，此快取跟著「目前登入的 Sandbox 帳號」走、不存在 App 自己的容器內，單純刪除重裝 App 不會清掉；ASC 的 Clear Purchase History 只清**伺服器端**紀錄，裝置本地快取要重新跟伺服器對一次（即 `AppStore.sync()`，也就是 Restore Purchase 按鈕本身做的事）才會更新。已回覆使用者建議流程：① 確認裝置上真的完全刪除 App（非 Xcode Run 覆蓋安裝）；② 裝置上把 Sandbox 帳號登出再登入一次；③ 重裝 App；④ 冷啟動可能仍顯示已解鎖屬預期（尚未強制同步）；⑤ 手動點 Restore Purchase（觸發真正的 `AppStore.sync()`）才是正確的驗證時機點，若這步之後仍顯示已解鎖才需要回頭查程式碼。**✅ 使用者已依此流程重測，PASS**：Clear Purchase History 後用 Sandbox 測試帳號點 Restore Purchase，確定無法還原（正確顯示沒有可還原的購買）；重新購買一次，確定又能正常移除廣告。**IAP／Restore 全部情境（正向購買、正向 Restore、反向 Restore、重複購買後再 Restore）至此全數 PASS**。**AdMob 測試裝置 ID 每次都不同**：使用者發現重測 Restore Purchase 時（需要整個刪除重裝 App）Google Console 印出的裝置 ID 每次都不一樣，追加不完。根因：Google 的裝置測試 ID 雜湊值跟著 `identifierForVendor`（IDFV）算，而 IDFV 有「該開發商所有 App 都從裝置移除後，下次重裝會重新產生」的特性，使用者這幾輪測試剛好一直在整個刪除重裝 App，導致 ID 一直變、用 `testDeviceIdentifiers` 註冊特定裝置 ID 的做法從根本上就跟這個測試情境衝突。**改用更根本的解法**：`AdBannerView.swift` 的 `AdUnitID` 改成 `#if DEBUG` 時直接用 Google 官方測試 Ad Unit ID（`ca-app-pub-3940256099942544/2934735716`，四個版位共用，Google 允許重複使用，不綁裝置、永久有效、不需要任何註冊步驟），Release/Archive 送審 build 維持原本 4 個正式 Ad Unit ID 不受影響；`JD2_LogbookApp.swift` 移除已無意義的 `testDeviceIdentifiers` 註冊程式碼。iOS + macOS build 皆通過。**⚠️ 尚待使用者重新 build 確認 DEBUG build 下四個版位都直接顯示 Google 官方測試廣告，不需要再手動加裝置 ID** |

---

## 使用方式

- 每項工作展開前，先跟使用者確認細節範圍（尤其 #1 需要等使用者提供 Apple 拒絕信完整內容）。
- 完成一項，狀態改為 ✅ 完成，並在「細節」欄補上實作摘要／commit 參照，比照 `V1_1_BACKLOG.md` 的紀錄方式。
- 若某項工作展開後發現屬於家族層共用（DiveKit／DiveImportKit）範圍，依 `../CLAUDE.md` 家族鐵律第 8 條「回報鏈」處理，不自行跨 repo 修改。

---

**文件版本：** v1.0 | **建立日期：** 2026-07-20 | **最後更新：** 2026-07-20
